# Nocodile 前端启动指南

## 📋 概述

Nocodile 前端使用 Next.js 16 构建。本指南将帮助您启动前端开发服务器。

## 🚀 启动方法

### 方法 1: 本地开发（推荐用于开发）

#### 步骤 1: 安装依赖

```bash
# 进入前端目录
cd /Users/lubitong/Desktop/noco2/Nocodile/frontend

# 安装依赖（选择一种方式）
npm install
# 或
pnpm install
# 或
yarn install
```

#### 步骤 2: 配置环境变量（可选）

创建 `.env.local` 文件（如果不存在）：

```bash
# 在 frontend 目录下
cat > .env.local << EOF
NEXT_PUBLIC_API_URL=http://localhost:8888
EOF
```

**注意**: 如果不设置，前端会使用默认值 `http://localhost:8888`，并自动尝试多个备用 URL。

#### 步骤 3: 启动开发服务器

```bash
# 使用 npm
npm run dev

# 或使用 pnpm
pnpm dev

# 或使用 yarn
yarn dev
```

#### 步骤 4: 访问前端

打开浏览器访问：
- **开发服务器**: http://localhost:3000

### 方法 2: 使用 Docker Compose

```bash
# 在项目根目录
cd /Users/lubitong/Desktop/noco2/Nocodile

# 启动前端（会自动启动依赖的后端和数据库）
docker-compose up frontend

# 或启动所有服务
docker-compose up
```

前端将在 http://localhost:3001 运行（根据 docker-compose.yml 配置）。

### 方法 3: 生产模式构建

```bash
# 1. 构建生产版本
cd /Users/lubitong/Desktop/noco2/Nocodile/frontend
npm run build

# 2. 启动生产服务器
npm start
```

## ⚙️ 配置说明

### 环境变量

前端使用以下环境变量：

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `NEXT_PUBLIC_API_URL` | `http://localhost:8888` | 后端 API 地址 |

### API 连接配置

前端会自动尝试以下后端 URL（按顺序）：
1. 环境变量 `NEXT_PUBLIC_API_URL`
2. `http://localhost:8888`
3. `http://host.docker.internal:8888` (Docker 环境)
4. `http://backend:8888` (Docker 内部网络)

## 🔍 常见问题

### 问题 1: 端口已被占用

**错误**: `Port 3000 is already in use`

**解决方案**:
```bash
# 方法 1: 使用其他端口
PORT=3001 npm run dev

# 方法 2: 查找并关闭占用端口的进程
lsof -ti:3000 | xargs kill -9
```

### 问题 2: 无法连接到后端

**错误**: `无法連接到任何後端服務`

**解决方案**:
1. 确保后端服务正在运行（http://localhost:8888）
2. 检查后端健康状态: `curl http://localhost:8888/health`
3. 检查环境变量 `NEXT_PUBLIC_API_URL` 是否正确
4. 查看浏览器控制台的网络请求错误

### 问题 3: 依赖安装失败

**错误**: `npm ERR!` 或 `pnpm ERR!`

**解决方案**:
```bash
# 清除缓存并重新安装
rm -rf node_modules package-lock.json
npm cache clean --force
npm install

# 或使用 pnpm
rm -rf node_modules pnpm-lock.yaml
pnpm store prune
pnpm install
```

### 问题 4: TypeScript 错误

**错误**: TypeScript 编译错误

**解决方案**:
```bash
# 检查 TypeScript 配置
npx tsc --noEmit

# 如果只是警告，可以继续运行（开发模式会显示警告但不会阻止运行）
```

## 📝 开发模式特性

开发模式 (`npm run dev`) 提供：
- ✅ 热重载（代码修改自动刷新）
- ✅ 详细的错误信息
- ✅ 快速刷新
- ✅ 开发工具支持

## 🏗️ 构建生产版本

```bash
# 1. 构建
npm run build

# 2. 启动生产服务器
npm start

# 生产服务器默认运行在 http://localhost:3000
```

## 🔗 相关链接

- **前端代码**: `/Users/lubitong/Desktop/noco2/Nocodile/frontend`
- **后端 API**: http://localhost:8888
- **API 文档**: 查看 `frontend/API_CONNECTION_GUIDE.md`

## ✅ 启动检查清单

- [ ] Node.js 已安装（推荐 v18+）
- [ ] 依赖已安装 (`npm install`)
- [ ] 后端服务正在运行（http://localhost:8888）
- [ ] 环境变量已配置（可选）
- [ ] 端口 3000 未被占用
- [ ] 浏览器可以访问 http://localhost:3000

## 🎯 快速启动命令

```bash
# 完整启动流程
cd /Users/lubitong/Desktop/noco2/Nocodile/frontend
npm install
npm run dev
```

然后在浏览器打开: http://localhost:3000

## 🐛 调试技巧

### 查看 API 连接状态

前端会在浏览器控制台显示 API 连接状态。打开开发者工具（F12）查看：
- 连接测试结果
- API 请求日志
- 错误信息

### 检查后端连接

```bash
# 测试后端健康检查
curl http://localhost:8888/health

# 测试后端 API
curl http://localhost:8888/test
```

## 📚 下一步

前端启动后，您可以：
1. 访问登录页面注册/登录
2. 创建新项目
3. 上传视频
4. 进行标注
5. 训练模型

祝您使用愉快！🎉

